source 'https://rubygems.org'

# Specify your gem's dependencies in project-instagram_api

gemspec

gem install sinatra
gem install instagram
gem install sinatra rake rack-test

group :test do
	gem install minitest
	gem install test-unit-capybara
end